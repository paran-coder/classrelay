# ClassRelay v2.6.2 Context Notes

## Goal
메일 템플릿의 `추가`와 `복제` 역할을 명확히 분리한다.

## Product rules
1. `+ 템플릿 추가` = 빈 신규 템플릿 작성.
2. `복제` = 기존 템플릿 제목/본문을 복사.
3. 새 템플릿은 이름, 사용 강의, 제목, 본문을 한 번에 입력한다.
4. 한 강의에는 전용 템플릿 하나만 연결한다.
5. 이미 연결된 강의를 새 템플릿에 연결할 때는 교체 확인이 필요하다.
6. 확인 후 다른 탭에서 연결이 바뀌면 저장하지 않는다.
7. 생성 성공 직후 새 템플릿을 자동 선택한다.
8. IndexedDB schema는 변경하지 않는다.
