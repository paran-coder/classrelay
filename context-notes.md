# ClassRelay v2.8.0 — Context Notes

## Goal
발송 흐름을 **Course-first Sending**으로 재설계한다. 한 번의 발송 작업에는 반드시 하나의 강의만 포함한다.

## Approved requirements
1. 메일/발송로그의 `발송 대상 선택`은 강의를 먼저 선택한다.
2. 강의를 선택한 뒤 해당 강의의 `발송 가능` 신청 건만 보여준다.
3. 강의 히스토리에서는 현재 강의가 이미 확정되어 있으므로 강의 선택 단계를 생략하고 바로 `이 강의 발송 대상`으로 진입한다.
4. 최종 발송 확인에는 강의명, 선택 인원, 템플릿, 녹화본 URL, 제목을 표시한다.
5. 한 사람이 여러 강의를 신청했어도 각 강의 신청 건을 별도 메일로 발송한다. 여러 강의를 하나의 발송 작업에 혼합하지 않는다.

## Safety / data constraints
- 기존 Form/입금/발송/CS 히스토리를 보존한다.
- 최초 발송 중복 방지와 `발송중`, `발송 확인 필요` 보호를 유지한다.
- 발송 가능 여부는 특정 강의 신청 건 기준으로 계산한다.
- 이번 버전에서는 IndexedDB schema를 변경하지 않는다.

## UI principle
강의가 현재 작업의 컨텍스트이며 `발송 가능`은 그 강의 안의 상태다. 강의를 다시 필터처럼 선택하게 하지 않는다.
