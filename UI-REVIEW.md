# ClassRelay v2.11.0 — UI Review

## Surface strategy
은행 내역 import는 Dashboard/Admin surface다. 기존 ClassRelay의 compact form, modal, table, button 시스템을 재사용하고 장식보다 `파일 선택 → 구조 지정 → 확인 → 저장`의 작업 순서가 분명하게 보이도록 했다.

## 은행 내역 가져오기 홈
- 저장된 템플릿이 있으면 `템플릿 선택 + 새 입금내역 파일`을 가장 먼저 보여준다.
- 새 형식은 `새 템플릿 만들기`로 분리한다.
- 템플릿 이름은 은행명 강제가 아니라 자유 이름을 사용한다.
- 저장 템플릿 삭제는 이미 가져온 입금내역을 삭제하지 않는다는 설명과 별도 확인을 거친다.
- 빈 샘플로 저장해 아직 실제 데이터 미리보기를 확인하지 않은 템플릿은 `첫 실제 파일에서 미리보기 확인 필요` 상태를 표시한다.

## 새 템플릿 wizard
- 상단 3단계: `1 샘플 파일 / 2 헤더·열 지정 / 3 미리보기·저장`.
- 필수 필드는 `입금 날짜 / 입금자명 / 입금액`으로 명확히 구분한다.
- `입금 시간`, `은행 고유번호`는 선택 항목으로 낮은 강조를 사용한다.
- 헤더 행 도움말은 계좌 정보/조회 기간이 상단에 있어도 실제 열 이름 행을 선택하면 된다고 설명한다.
- 5행 미리보기는 `원본 행 / 입금일시 / 입금자명 / 입금액 / 은행 고유번호`만 보여줘 사용자가 매핑 결과를 바로 검증하게 한다.
- 실제 입금 행이 없는 샘플은 오류로 끝내지 않고 `템플릿만 저장`을 허용하고 첫 실제 파일에서 미리보기 확인을 요구한다.

## 오류 예방
- 저장 템플릿과 다음 파일의 매핑 헤더 위치/이름이 다르면 즉시 import하지 않는다.
- 자동 추정은 시작값일 뿐이며 사용자가 직접 열을 선택하고 미리보기로 확인한다.
- 같은 템플릿 이름을 조용히 덮어쓰지 않는다.
- 외부 sheet만 참조하는 HTML `.xls`는 표를 추측해 import하지 않고 원본 파일 안에 데이터가 없다고 설명한다.

## Responsive visual QA
실제 `assets/styles.css`와 은행 템플릿 wizard markup을 Chromium으로 렌더했다.

| Surface | Viewport | Document overflow | Modal/body overflow | 처리 |
|---|---:|---:|---:|---|
| 은행 템플릿 wizard | 1440×1000 | 0px | 0px | 정상 |
| 은행 템플릿 wizard | 390×844 | 0px | 0px | 정상 |

초기 모바일 QA에서 native file input이 modal 내부 폭보다 넓어지는 문제를 발견했고, bank import file input에 `width:100%; max-width:100%; min-width:0` containment를 추가한 뒤 재검수했다. 미리보기 표는 의도적으로 `.table-wrap` 안에서만 수평 스크롤한다.

## Review result
사용자가 매번 은행별 규칙을 학습하는 대신 본인의 실제 파일 구조를 한 번 지정하고 재사용하는 모델이 ClassRelay의 local-first 성격과 잘 맞는다. 자동 은행 판별보다 설정 과정은 한 단계 더 있지만, 잘못된 열을 자동 확정하는 위험과 은행 UI 변경에 대한 유지보수 비용을 줄인다.
