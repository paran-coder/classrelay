# ClassRelay v2.8.0 — User Manual

## 1. 기본 운영 흐름

1. 강의를 등록합니다.
2. 새 강의의 Google Form을 연결합니다.
3. `전체 폼 동기화` 또는 강의 히스토리의 `이 강의 폼 동기화`로 신청자를 가져옵니다.
4. 은행 CSV를 가져오면 신규 입금만 누적되고 즉시 자동매칭합니다.
5. 입금확인과 확인필요 건을 검토합니다.
6. 메일 발송은 **강의를 먼저 선택**한 뒤 해당 강의의 발송 가능한 신청 건만 선택합니다.
7. 발송 후 성공/실패/확인필요 상태와 당시 실제 메일 내용을 CS 히스토리에 보존합니다.

## 2. 메일 발송 — Course-first Sending

### 메일 / 발송로그에서 시작

`발송 대상 선택`을 누르면 강의 목록이 먼저 열립니다. 각 강의에는 `발송 가능 n명`이 표시됩니다. 이 숫자는 다음 조건을 모두 만족한 신청 건만 포함합니다.

- 입금확인(MATCHED 또는 MANUAL_CONFIRMED)
- 유효한 이메일 주소
- 해당 강의의 녹화본 URL 존재
- 사용할 메일 템플릿 존재
- 최초 발송 전 상태
- `발송중`/`발송 확인 필요` 상태가 아님

강의를 선택하면 발송 대상 화면에서 강의는 이미 고정되어 표시됩니다. 사용자는 강의를 다시 선택하지 않고 해당 강의의 발송 가능 신청자만 체크합니다.

### 강의 히스토리에서 시작

강의 히스토리의 `이 강의 발송 대상 n명`을 누르면 현재 강의가 이미 확정되어 있으므로 강의 선택 화면을 건너뜁니다.

### 한 번의 발송 = 한 강의

여러 강의의 신청자를 한 번의 발송 작업에 섞지 않습니다. 같은 사람이 강의 A와 강의 B를 모두 신청했더라도 A 메일과 B 메일은 각각 별도 신청 건/별도 발송으로 처리합니다.

### 최종 발송 확인

실제 Gmail 호출 전 확인창에서 다음을 확인합니다.

- 강의명
- 선택 인원
- 사용할 메일 템플릿
- 녹화본 URL
- 제목 템플릿

이후 각 신청자에게 개별 Gmail 메일을 순서대로 발송합니다.

## 3. 메일 템플릿

- `+ 템플릿 추가`: 빈 템플릿 새 작성
- `복제`: 현재 템플릿 내용 복사
- 기본 템플릿: 전용 템플릿이 없는 강의에 자동 적용
- 전용 템플릿: 특정 강의에 연결
- 변수: `{{이름}}`, `{{강의명}}`, `{{녹화본URL}}`, `{{신청번호}}`, `{{금액}}`

## 4. Form 동기화

- 대시보드: `폼 추가` / `전체 폼 동기화`
- 강의 히스토리: `이 강의 폼 동기화`
- 새 강의는 새 Form과 연결하는 것을 원칙으로 합니다.
- 재동기화해도 기존 입금·발송·CS 기록은 초기화되지 않습니다.

## 5. 입금 CSV

CSV는 교체가 아니라 누적입니다. 기존 입금을 지우지 않고 신규 행만 추가합니다. 자동매칭 기본 기간은 신청 전 1일~신청 후 7일이며 이름·금액·날짜 조건이 유일한 1:1일 때만 자동확정합니다.

## 6. CS

강의 히스토리에서 신청자 행을 선택하면 우측 패널에서 발송횟수, 최종 발송, 입금, CS 메모, 발송 당시 내용을 확인할 수 있습니다. 재발송은 해당 신청 건의 강의를 기준으로 실행됩니다.

## 7. 데이터 보존

Form 재동기화, 추가 CSV 업로드, 템플릿 수정은 기존 발송/입금/CS 히스토리를 초기화하지 않습니다. 설정에서 정기적으로 JSON 백업을 내보내는 것을 권장합니다.
