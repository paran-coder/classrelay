# ClassRelay v2.6.0 User Manual

## 1. 처음 설정
1. Google Cloud에서 본인 OAuth Web Client를 만듭니다.
2. Google Forms API와 Gmail API를 활성화합니다.
3. ClassRelay 설정에서 OAuth Client ID를 저장합니다.
4. Google Form 편집 URL을 연결하고 질문 매핑을 확인합니다.
5. 강의를 등록하고 가격과 YouTube 녹화본 URL을 저장합니다.

## 2. 메일 템플릿 관리
`메일 / 발송로그`에서 여러 템플릿을 관리합니다.

### 템플릿 추가
`템플릿 추가`를 눌러 새 템플릿을 만듭니다.

### 기본 템플릿
강의에 별도 템플릿이 지정되지 않았을 때 사용하는 템플릿입니다. 기본 템플릿은 항상 하나 유지됩니다.

### 강의별 템플릿
템플릿 편집 화면의 `사용 강의`에서 강의를 체크하면 그 강의는 해당 템플릿을 사용합니다. 체크를 해제하면 기본 템플릿으로 돌아갑니다.

강의 편집 화면에서도 `메일 템플릿`을 직접 선택할 수 있습니다.

### 변수
- `{{이름}}`
- `{{강의명}}`
- `{{녹화본URL}}`
- `{{신청번호}}`
- `{{금액}}`

`미리보기`에서 샘플 값으로 렌더링 결과를 확인할 수 있습니다.

### 복제 / 삭제
- `복제`: 현재 템플릿을 복사해 새 템플릿을 만듭니다.
- `삭제`: 기본 템플릿은 삭제할 수 없습니다. 다른 템플릿을 기본으로 지정한 뒤 삭제해야 합니다.
- 삭제되는 템플릿을 사용하던 강의는 자동으로 연결이 해제되어 기본 템플릿을 사용합니다.

## 3. 신청자 동기화
Google Form을 다시 동기화해도 기존 입금확인, 발송상태, 발송횟수, CS 메모, 수동 수정값은 초기화되지 않습니다.

## 4. CSV 입금 관리
은행 CSV를 추가로 올려도 기존 입금내역을 지우지 않습니다. 신규 입금만 누적하고 즉시 자동매칭합니다.

기본 자동매칭 기간은 신청 전 1일 ~ 신청 후 7일입니다.

## 5. 메일 발송
신청자의 강의에 연결된 템플릿을 먼저 사용하고, 별도 연결이 없으면 기본 템플릿을 사용합니다.

발송 확인창을 연 뒤 다른 탭에서 강의 또는 템플릿이 변경되면 해당 신청자는 그 발송 작업에서 제외됩니다. 화면을 다시 확인한 뒤 발송합니다.

## 6. 발송 당시 내용 확인
성공 발송 로그에는 당시 실제로 전송한 제목, 본문, 녹화본 URL, 수신 이메일, 템플릿, 강의가 별도 사본으로 저장됩니다.

`발송 내용 보기 →`를 누르면 과거 템플릿이 나중에 수정되어도 당시 내용을 그대로 확인할 수 있습니다.

이 기능은 다음 위치에서 사용할 수 있습니다.
- 메일 / 발송로그
- 신청자 우측 상세 패널의 최근 활동
- 강의 히스토리 우측 CS 패널의 발송 / CS 활동

## 7. 재발송
재발송은 현재 시점의 강의 설정과 현재 연결된 템플릿을 사용합니다. 이전 발송 내용은 조회용 히스토리로 보존됩니다.

## 8. 백업
백업 JSON에는 설정, 강의, 신청자, 입금내역, 로그, 메일 템플릿 및 발송 당시 내용이 포함됩니다. 개인정보가 포함될 수 있으므로 안전한 위치에 보관합니다.
