# ClassRelay v2.8.3 — User Manual

## 1. 기본 운영 흐름

1. 강의를 등록합니다.
2. 새 강의를 저장한 직후 Google Form을 연결하거나 대시보드의 `폼 추가`를 사용합니다.
3. `전체 폼 동기화` 또는 강의 히스토리의 `이 강의 폼 동기화`로 신청자를 가져옵니다.
4. 은행 CSV를 가져오면 신규 입금만 누적되고 즉시 자동매칭합니다.
5. 입금확인과 확인필요 건을 검토합니다.
6. 메일 발송은 **강의를 먼저 확정**한 뒤 해당 강의의 발송 가능한 신청 건만 선택합니다.
7. 발송 후 성공/실패/확인필요 상태와 당시 실제 메일 내용을 CS 히스토리에 보존합니다.
8. 운영 종료 전 또는 주기적으로 JSON 백업을 내보냅니다.

## 2. Form 연결과 동기화 용어

- `폼 추가`: 새 Google Form 편집 URL을 특정 강의에 연결
- `전체 폼 동기화`: 사용 중인 강의의 활성 Form 전체를 순차 동기화
- `이 강의 폼 동기화`: 현재 강의 Form 하나만 동기화

ClassRelay는 Google Drive 전체 Form 목록을 불러오지 않습니다. 새 강의에 사용할 Form의 편집 URL을 직접 연결합니다. 같은 응답을 다시 동기화해도 기존 입금·발송·CS 기록은 유지됩니다.

## 3. 메일 발송 — Course-first Sending

### 메일 / 발송로그에서 시작

`발송 대상 선택`을 누르면 강의 목록이 먼저 열립니다. 각 강의에는 실제 `발송 가능 n명`이 표시됩니다. 이 숫자는 다음 조건을 모두 만족한 신청 건만 포함합니다.

- 입금확인(MATCHED 또는 MANUAL_CONFIRMED)
- 유효한 이메일 주소
- 해당 강의의 녹화본 URL 존재
- 사용할 메일 템플릿 존재
- 최초 발송 전 상태
- `발송중`/`발송 확인 필요` 상태가 아님

강의를 선택하면 발송 대상 화면에서 강의는 고정된 작업 컨텍스트로 표시됩니다. 사용자는 강의를 다시 선택하지 않고 해당 강의의 발송 가능 신청자만 체크합니다.

### 강의 히스토리에서 시작

강의 히스토리의 `이 강의 발송 대상 n명`을 누르면 현재 강의가 이미 확정되어 있으므로 강의 선택 화면을 건너뜁니다.

### 한 번의 발송 = 한 강의

여러 강의의 신청자를 한 번의 발송 작업에 섞지 않습니다. 같은 사람이 강의 A와 강의 B를 모두 신청했더라도 A 메일과 B 메일은 별도 신청 건/별도 발송으로 처리합니다.

### 최종 발송 확인

실제 Gmail 호출 전 확인창에서 강의명, 선택 인원, 사용할 메일 템플릿, 녹화본 URL, 제목 템플릿을 확인합니다.

## 4. 메일 템플릿과 발송로그

- `+ 템플릿 추가`: 빈 템플릿 새 작성
- `복제`: 현재 템플릿의 제목/본문 복사
- 기본 템플릿: 전용 템플릿이 없는 강의에 자동 적용
- 전용 템플릿: 특정 강의에 연결
- 변수: `{{이름}}`, `{{강의명}}`, `{{녹화본URL}}`, `{{신청번호}}`, `{{금액}}`
- 메일 화면 하단의 전체 폭 발송내역에서 발송일시, 신청자, 강의, 템플릿, 동작을 확인
- `발송 내용 보기`에서 성공 발송 당시 제목·본문·URL snapshot 확인

## 5. 입금 CSV

CSV는 교체가 아니라 누적입니다. 기존 입금을 지우지 않고 신규 행만 추가합니다. 자동매칭 기본 기간은 신청 전 1일~신청 후 7일이며 이름·금액·날짜 조건이 유일한 1:1일 때만 자동확정합니다.

## 6. CS

강의 히스토리에서 신청자 행을 선택하면 우측 패널에서 발송횟수, 최종 발송, 입금, CS 메모, 발송 당시 내용을 확인할 수 있습니다. 재발송은 해당 신청 건의 강의를 기준으로 실행됩니다.

## 7. 녹화본 URL

ClassRelay는 YouTube API를 사용하지 않고 강의에 저장된 URL을 메일에 넣습니다. YouTube 일부공개는 링크를 가진 사람이 시청·재공유할 수 있으므로 접근통제 수단으로 보지 않습니다. 비공개 동영상은 YouTube에서 지정한 사용자만 볼 수 있어 단순 URL 발송 흐름과 다릅니다.

## 8. 데이터 보존

Form 재동기화, 추가 CSV 업로드, 템플릿 수정은 기존 발송/입금/CS 히스토리를 초기화하지 않습니다. 설정에서 정기적으로 JSON 백업을 내보내는 것을 권장합니다.


## 화면 색상 역할

- 검정: 저장, 생성, 발송 같은 주요 실행 버튼
- 파랑 `#2563EB`: 선택된 행, 활성 필터, 포커스, 링크
- 녹색/황색/적색: 성공, 확인 필요, 실패 상태

파랑은 선택과 이동을 설명하는 보조 색이며 주요 발송/저장 CTA를 대체하지 않습니다.


## 화면 색상 규칙

ClassRelay의 요약 카드에서 숫자는 데이터이므로 검정으로 표시됩니다. 파란색 `보기 →` 문구는 이동/필터 동작을 뜻하며, 현재 적용 중인 필터 카드만 연한 파란색 배경으로 표시됩니다.
